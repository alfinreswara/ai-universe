'use client';
import Link from 'next/link';import { usePathname } from 'next/navigation';import { useEffect,useState } from 'react';import { Orbit,Search,Menu,X } from 'lucide-react';import { useUniverse } from '@/store/universe';import SearchDialog from './SearchDialog';
export default function Header(){const path=usePathname(),setSearch=useUniverse(s=>s.setSearch);const [menu,setMenu]=useState(false);useEffect(()=>{const key=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();setSearch(!useUniverse.getState().searchOpen);}};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key);},[setSearch]);return <><a href="#main" className="skip-link">Skip to content</a><header className="topbar"><Link href="/" className="brand" aria-label="AI Universe home"><Orbit size={26} strokeWidth={1.25}/><span>AI UNIVERSE<span className="brand-point">®</span></span></Link><nav className={menu?'main-nav open':'main-nav'} aria-label="Main navigation">{['Explore','Models','Providers','Learn'].map(item=><Link key={item} className={(path.startsWith('/'+item.toLowerCase())||(path==='/'&&item==='Explore'))?'active':''} href={'/'+item.toLowerCase()} onClick={()=>setMenu(false)}>{item}</Link>)}</nav><div className="header-actions"><button className="search-trigger" aria-label="Search universe" onClick={()=>setSearch(true)}><Search size={17}/><span>Search universe</span><kbd>⌘ K</kbd></button><button className="icon-button mobile-menu" aria-label="Toggle navigation" aria-expanded={menu} onClick={()=>setMenu(!menu)}>{menu?<X size={20}/>:<Menu size={20}/>}</button></div></header><SearchDialog/></>;}
